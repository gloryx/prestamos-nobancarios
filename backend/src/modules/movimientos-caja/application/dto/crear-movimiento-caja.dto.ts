import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger'; import { Type } from 'class-transformer'; import { IsDateString, IsEnum, IsInt, IsNumber, IsOptional, IsPositive, IsString, Matches, Max, MaxLength, Min } from 'class-validator'; import { ConceptoMovimientoCaja } from '../../domain/enums/concepto-movimiento-caja.enum';
export class CrearMovimientoCajaDto { @ApiProperty({enum:ConceptoMovimientoCaja, description:'Manual concepts only: APORTE_CAPITAL, RETIRO, GASTO, AJUSTE_ENTRADA or AJUSTE_SALIDA.'}) @IsEnum(ConceptoMovimientoCaja) concepto!:ConceptoMovimientoCaja; @ApiProperty({example:50000, description:'Positive amount with at most two decimal places.'}) @Type(()=>Number) @IsNumber({maxDecimalPlaces:2}) @IsPositive() monto!:number; @ApiProperty({example:'2026-08-31',format:'date', description:'Economic date in America/Costa_Rica; today or earlier, on/after financial opening, and in an open period.'}) @Matches(/^\d{4}-\d{2}-\d{2}$/) @IsDateString({strict:true}) fecha!:string; @ApiPropertyOptional({example:2, minimum:1, maximum:32767, description:'Active FormaPago ID. Required for APORTE_CAPITAL, RETIRO and GASTO; optional for adjustments.'}) @IsOptional() @Type(()=>Number) @IsInt() @Min(1) @Max(32767) formaPagoId?:number|null; @ApiPropertyOptional({example:'Aporte de caja inicial', description:'Required after trim for adjustments.'}) @IsOptional() @IsString() @MaxLength(1000) observaciones?:string|null; }
