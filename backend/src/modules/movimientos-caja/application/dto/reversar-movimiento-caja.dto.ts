import { ApiProperty } from '@nestjs/swagger'; import { IsDateString, IsString, Matches, MaxLength, MinLength } from 'class-validator'; export class ReversarMovimientoCajaDto { @ApiProperty({example:'2026-08-31',format:'date'}) @Matches(/^\d{4}-\d{2}-\d{2}$/) @IsDateString({strict:true}) fecha!:string; @ApiProperty({example:'Corrección autorizada'}) @IsString() @MinLength(1) @MaxLength(1000) observaciones!:string; }
