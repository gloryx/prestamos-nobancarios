import { ConceptoMovimientoCaja } from '../enums/concepto-movimiento-caja.enum';
import { TipoMovimientoCaja } from '../enums/tipo-movimiento-caja.enum';
export interface DatosMovimientoCaja { tipo: TipoMovimientoCaja; concepto: ConceptoMovimientoCaja; monto: number; fecha: Date; observaciones: string | null; pagoId?: number | null; formaPagoId?: number | null; prestamoId?: number | null; refinanciamientoId?: number | null; movimientoReversadoId?: number | null; usuarioId: number; }
export class MovimientoCaja { constructor(public id: number | null, public tipo: TipoMovimientoCaja, public concepto: ConceptoMovimientoCaja, public monto: number, public fecha: Date, public observaciones: string | null, public pagoId: number | null, public prestamoId: number | null, public refinanciamientoId: number | null, public movimientoReversadoId: number | null, public usuarioId: number, public fechaCreacion: Date, public formaPagoId: number | null = null) {} static crear(d: DatosMovimientoCaja) { if (!(d.monto > 0)) throw new Error('El monto debe ser mayor que cero.'); return new MovimientoCaja(null, d.tipo, d.concepto, d.monto, d.fecha, d.observaciones?.trim() || null, d.pagoId ?? null, d.prestamoId ?? null, d.refinanciamientoId ?? null, d.movimientoReversadoId ?? null, d.usuarioId, new Date(), d.formaPagoId ?? null); } }
