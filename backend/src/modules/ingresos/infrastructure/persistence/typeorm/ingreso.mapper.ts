import { Ingreso } from '../../../domain/entities/ingreso'; import { IngresoOrmEntity } from './ingreso.orm-entity';
export class IngresoMapper { static toDomain(e: IngresoOrmEntity) { return new Ingreso(e.id, e.fuenteIngresoId, e.fecha, e.monto, e.descripcion, e.usuarioId, e.fechaCreacion, e.fechaActualizacion, e.fuenteIngreso && { id: e.fuenteIngreso.id, nombre: e.fuenteIngreso.nombre, activo: e.fuenteIngreso.activo }, e.usuario && { id: e.usuario.id, nombreCompleto: e.usuario.nombreCompleto }); } static toOrm(v: Ingreso) { const e = new IngresoOrmEntity(); if (v.id !== null) e.id = v.id; e.fuenteIngresoId = v.fuenteIngresoId; e.fecha = v.fecha; e.monto = v.monto; e.descripcion = v.descripcion; e.usuarioId = v.usuarioId; return e; } }
